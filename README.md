# robinphoto juniors

검단중앙교회 소년부 사진 앨범 정적 사이트입니다. `robinphoto/data` 저장소의 JSON과 커버 이미지를 읽어 GitHub Pages에서 표시합니다.

## 로컬 확인

`/Users/robin/Developer/robinphoto`를 웹 서버의 루트로 실행한 뒤 `/juniors/`를 엽니다. 로컬에서는 형제 디렉토리인 `data`의 파일을 사용합니다.

## 테스트

```sh
npm test
```
